{{define "main"}}
<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8">
		<link rel="icon" href="../assets/images/favicon.png" type="image/png">
		<title>{{.Title}}</title>
	</head>
	<body>
	   <p>Hello world !</p>
       <div>
            {{ range $key, $value := .ListOfEmojisFromGitHub }}
                <li>{{ $key }}: <img src='{{ $value }}' alt='{{ $key }}'></li>
            {{ end }}
       </div>
	</body>
</html>
{{end}}