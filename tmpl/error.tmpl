{{define "error"}}

	{{template "header"}}

				<title>{{.Title}}</title>
            </head>
        <body>

			<div id="error_dialog" title="Basic dialog">
	   			<p class="error_displayer">Error occured...</p>

				{{template "footer"}}

			</div>

	{{template "end"}}

{{end}}